<%-include('partials/header', {DocumentTitle:'Shortened URL'})%>

<div class="container">
    <div class="card m-4 text-center">
        <div class="card-body">
          <h3 class="card-title">Your shortened URL</h3>
          <p class="card-text">
            <input type="text" class="form-control text-center" value="<%= shortURL %>">
            <h5> Original URL: <%= fullURL %> </h5>
          </p>
        </div>
      </div>
</div>

<%-include('partials/footer')%>